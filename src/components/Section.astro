---
import type { Translation } from "@/i18n";
import type { ImageMetadata } from "astro";

import { Card, CardHeader, CardContent, CardFooter } from "@/components/ui/card";
import { Image } from "astro:assets";
import BookDemoButton from "@/components/BookDemoButton.astro";
import GSAPSection from "@/components/ui/gsap-section";
import LottiePlayer from "@/components/LottiePlayer";

interface SectionCard {
    title?: string;
    description: string;
    image?: ImageMetadata;
    lottie?: object;
    imageClass?: string;
    cardClass?: string;
    altText?: string;
    showBookDemo?: boolean;
}

interface Props {
    t: Translation;
    sectionTitle: string;
    sectionDescription: string;
    leftCard: SectionCard;
    rightCard: SectionCard;
    showCTACard?: boolean;
    ctaPosition?: "left" | "right";
    leftCardHeight?: string;
    rightCardHeight?: string;
}

const {
    t,
    sectionTitle,
    sectionDescription,
    leftCard,
    rightCard,
    showCTACard = false,
    ctaPosition = "right",
    leftCardHeight = "h-80 md:h-110",
    rightCardHeight = "h-80 md:h-110",
} = Astro.props;

const hasLeftCardSlot = Astro.slots.has("leftCardVisual");
const hasRightCardSlot = Astro.slots.has("rightCardVisual");
---

<GSAPSection client:load className="flex flex-col gap-16 items-center">
    <div class="flex flex-col gap-4 text-center px-4">
        <h2 class="text-xl font-bold">{sectionTitle}</h2>
        <p class="text-muted-foreground max-w-2xl mx-auto">
            {sectionDescription}
        </p>
    </div>
    <div class="grid gap-4 px-4 text-sm sm:grid-cols-2 w-full">
        {
            showCTACard && ctaPosition === "left" ? (
                <div class="flex flex-col-reverse md:flex-col gap-4 h-120 sm:h-140">
                    <Card className="flex min-h-fit rounded bg-gradient-primary">
                        <CardContent className="flex items-center flex-col sm:flex-row gap-4 p-8 justify-between w-full">
                            <p class="max-w-sm text-white font-semibold text-center sm:text-left">
                                {t.setup.bookADemoCTA}
                            </p>
                            <BookDemoButton t={t} variant="secondary" />
                        </CardContent>
                    </Card>
                    <Card
                        className={`flex flex-col items-center rounded bg-card text-center ${leftCard.lottie || hasLeftCardSlot ? "overflow-x-hidden overflow-y-visible sm:overflow-hidden" : "overflow-hidden"} ${leftCard.cardClass || ""}`}
                    >
                        <CardHeader className="flex flex-col items-center gap-4">
                            {leftCard.title && <h3 class="text-xl font-bold">{leftCard.title}</h3>}
                            <p class="max-w-md">{leftCard.description}</p>
                        </CardHeader>
                        <CardFooter>
                            {hasLeftCardSlot ? (
                                <slot name="leftCardVisual" />
                            ) : leftCard.lottie ? (
                                <LottiePlayer
                                    client:only="react"
                                    animationData={leftCard.lottie}
                                    className={leftCard.imageClass || "w-full"}
                                />
                            ) : leftCard.image ? (
                                <Image
                                    src={leftCard.image}
                                    alt={leftCard.altText || leftCard.title || t.altText.heroImage}
                                    class={leftCard.imageClass || "w-full"}
                                />
                            ) : null}
                        </CardFooter>
                    </Card>
                </div>
            ) : (
                <Card
                    className={`flex flex-col items-center gap-6 rounded bg-card text-center ${leftCard.lottie || hasLeftCardSlot ? "overflow-x-hidden overflow-y-visible sm:overflow-hidden" : `overflow-hidden ${leftCardHeight}`} ${leftCard.cardClass || ""}`}
                >
                    <CardHeader className="flex flex-col items-center gap-4">
                        {leftCard.title && <h3 class="text-xl font-bold">{leftCard.title}</h3>}
                        <p class="max-w-md">{leftCard.description}</p>
                    </CardHeader>
                    <CardFooter>
                        {hasLeftCardSlot ? (
                            <slot name="leftCardVisual" />
                        ) : leftCard.lottie ? (
                            <LottiePlayer
                                client:only="react"
                                animationData={leftCard.lottie}
                                className={leftCard.imageClass || "w-full"}
                            />
                        ) : leftCard.image ? (
                            <Image
                                src={leftCard.image}
                                alt={leftCard.altText || leftCard.title || t.altText.heroImage}
                                class={leftCard.imageClass || "w-full"}
                            />
                        ) : null}
                    </CardFooter>
                </Card>
            )
        }
        {
            showCTACard && ctaPosition === "right" ? (
                <div class="flex flex-col-reverse md:flex-col gap-4 sm:h-140">
                    <Card className="flex min-h-fit rounded bg-gradient-primary">
                        <CardContent className="flex items-center flex-col sm:flex-row gap-4 p-8 justify-between w-full">
                            <p class="max-w-sm text-white font-semibold text-center sm:text-left">
                                {t.setup.bookADemoCTA}
                            </p>
                            <BookDemoButton t={t} variant="secondary" />
                        </CardContent>
                    </Card>
                    <Card
                        className={`flex flex-col items-center rounded bg-card text-center ${rightCard.lottie || hasRightCardSlot ? "overflow-visible" : "overflow-hidden"} ${rightCard.cardClass || ""}`}
                    >
                        <CardHeader className="flex flex-col items-center gap-4">
                            {rightCard.title && (
                                <h3 class="text-xl font-bold">{rightCard.title}</h3>
                            )}
                            <p class="max-w-md">{rightCard.description}</p>
                            {rightCard.showBookDemo && (
                                <BookDemoButton t={t} className="mt-2" />
                            )}
                        </CardHeader>
                        <CardFooter className={hasRightCardSlot ? "flex-col items-stretch w-full min-h-0" : ""}>
                            {hasRightCardSlot ? (
                                <slot name="rightCardVisual" />
                            ) : rightCard.lottie ? (
                                <LottiePlayer
                                    client:only="react"
                                    animationData={rightCard.lottie}
                                    className={rightCard.imageClass || "w-full"}
                                />
                            ) : rightCard.image ? (
                                <Image
                                    src={rightCard.image}
                                    alt={rightCard.altText || rightCard.title || t.altText.heroImage}
                                    class={rightCard.imageClass || "w-full"}
                                />
                            ) : null}
                        </CardFooter>
                    </Card>
                </div>
            ) : (
                <Card
                    className={`flex flex-col items-center gap-6 rounded bg-card text-center ${rightCard.lottie || hasRightCardSlot ? "overflow-visible" : `overflow-hidden ${rightCardHeight}`} ${rightCard.cardClass || ""}`}
                >
                    <CardHeader className="flex flex-col items-center gap-4">
                        {rightCard.title && <h3 class="text-xl font-bold">{rightCard.title}</h3>}
                        <p class="max-w-md">{rightCard.description}</p>
                        {rightCard.showBookDemo && (
                            <BookDemoButton t={t} className="mt-2" />
                        )}
                    </CardHeader>
                    <CardFooter className={hasRightCardSlot ? "flex-col items-stretch w-full min-h-0" : ""}>
                        {hasRightCardSlot ? (
                            <slot name="rightCardVisual" />
                        ) : rightCard.lottie ? (
                            <LottiePlayer
                                client:only="react"
                                animationData={rightCard.lottie}
                                className={rightCard.imageClass || "w-auto"}
                            />
                        ) : rightCard.image ? (
                            <Image
                                src={rightCard.image}
                                alt={rightCard.altText || rightCard.title || t.altText.heroImage}
                                class={rightCard.imageClass || "w-auto"}
                            />
                        ) : null}
                    </CardFooter>
                </Card>
            )
        }
    </div>
</GSAPSection>
