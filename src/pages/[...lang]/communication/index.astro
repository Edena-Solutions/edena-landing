---
import { translations, localeStaticPaths, localePath } from "@/i18n/index.ts";
import Layout from "@/layouts/main.astro";
import Hero from "@/components/Hero.astro";
import Section from "@/components/Section.astro";
import GSAPSection from "@/components/ui/gsap-section";
import Features from "@/components/Features";
import ProductFAQs from "@/components/ProductFAQs.astro";
import SoftwareApplicationSchema from "@/components/StructuredData/SoftwareApplication.astro";
import FAQPageSchema from "@/components/StructuredData/FAQPage.astro";
import { ogFromAsset } from "@/lib/og-image";
import heroImage from "@/assets/img/screenshots/communication/communication_hero.png";
import heroImageDark from "@/assets/img/screenshots/communication/dark/communication_hero.png";
import secureAppImage from "@/assets/img/screenshots/communication/secure_app.png";
import secureAppImageDark from "@/assets/img/screenshots/communication/dark/secure_app.png";
import internalChatImage from "@/assets/img/screenshots/communication/internal_chat.png";
import internalChatImageDark from "@/assets/img/screenshots/communication/dark/internal_chat.png";
import communicationImage from "@/assets/img/screenshots/communication/communication.png";
import communicationImageDark from "@/assets/img/screenshots/communication/dark/communication.png";

export function getStaticPaths() {
    return localeStaticPaths();
}

const { lang } = Astro.props;
const t = translations[lang as keyof typeof translations];
const m = t.productModules.communication;

const icons = ["MessagesSquare", "Megaphone", "Bell", "LayoutGrid", "Languages", "Archive"];
// Related pages per feature card (avoids 6x self-links).
const featureLinks = ["/app", "/workflows", "/app", "/families", "/nurseries", "/dashboard"];
const features = m.features.map((f, i) => ({
    icon: icons[i],
    title: f.title,
    description: f.description,
    navigateTo: localePath(lang, featureLinks[i] ?? "/demo"),
}));
---

<Layout
    content={{
        title: m.metaTitle,
        description: m.metaDescription,
        ...ogFromAsset(communicationImage, Astro.site),
    }}
    lang={lang}
>
    <GSAPSection client:load className="grid gap-16" animationType="opacity">
        <Hero
            t={t}
            showBookDemoInHero={true}
            mainTitle={m.mainTitle}
            mainDescription={m.mainDescription}
            heroImage={heroImage}
            heroImageDark={heroImageDark}
            leftCard={{
                title: m.heroLeftTitle,
                description: m.heroLeftDescription,
                showButton: true,
            }}
            rightCard={{
                title: m.heroRightTitle,
                description: m.heroRightDescription,
                image: secureAppImage,
                imageDark: secureAppImageDark,
                imageClass: "w-1/3 md:w-full mx-auto object-contain md:h-100",
                altText: m.altHero,
            }}
        />
        <Features
            client:load
            t={t}
            features={features}
            title={m.featuresTitle}
            description={m.featuresDescription}
        />
        <Section
            t={t}
            sectionTitle={m.sectionTitle}
            sectionDescription={m.sectionDescription}
            leftCard={{
                title: m.section1Title,
                description: m.section1Description,
                image: internalChatImage,
                imageDark: internalChatImageDark,
                imageClass: "w-1/3 md:w-full mx-auto object-contain md:h-100",
                altText: m.altSection1,
            }}
            rightCard={{
                title: m.section2Title,
                description: m.section2Description,
                image: communicationImage,
                imageDark: communicationImageDark,
                imageClass: "w-full mx-auto object-contain md:h-100",
                altText: m.altSection2,
            }}
        />
        <ProductFAQs t={t} title={m.faqTitle} description={m.faqDescription} faqs={m.faqs} />
    </GSAPSection>
    <SoftwareApplicationSchema
        slot="structured-data"
        name="Edena - Comunicación"
        description={m.metaDescription}
        applicationCategory="EducationalApplication"
        operatingSystem={["Web", "iOS", "Android"]}
        url={`https://edena.es/${lang}/communication`}
        lang={lang}
    />
    <FAQPageSchema slot="structured-data" faqs={m.faqs} translations={{}} lang={lang} />
</Layout>
