---
import { translations, languages } from "@/i18n/index.ts";
import Layout from "@/layouts/main.astro";
import Hero from "@/components/Hero.astro";
import Section from "@/components/Section.astro";
import GSAPSection from "@/components/ui/gsap-section";
import Features from "@/components/Features";
import ProductFAQs from "@/components/ProductFAQs.astro";
import SoftwareApplicationSchema from "@/components/StructuredData/SoftwareApplication.astro";
import ShopAppAnimation from "@/components/ShopAppAnimation";
import { ogFromAsset } from "@/lib/og-image";
import shopMain from "@/assets/img/screenshots/app/shop_main.png";
import shopItem from "@/assets/img/screenshots/app/shop_item.png";
import heroImage from "@/assets/img/screenshots/shop/shop_hero.png";
import heroImageDark from "@/assets/img/screenshots/shop/dark/shop_hero.png";

export function getStaticPaths() {
    return languages.map((lang) => ({
        params: { lang },
        props: { lang },
    }));
}

const { lang } = Astro.props;
const t = translations[lang as keyof typeof translations];
const m = t.productModules.shop;

const icons = ["Store", "CreditCard", "Package", "Shapes", "Smartphone", "BarChart2"];
const features = m.features.map((f, i) => ({
    icon: icons[i],
    title: f.title,
    description: f.description,
    navigateTo: `/${lang}/shop`,
}));
---

<Layout
    content={{
        title: m.metaTitle,
        description: m.metaDescription,
        ...ogFromAsset(shopMain, Astro.site),
    }}
    lang={lang}
>
    <GSAPSection client:load className="grid gap-16" animationType="opacity">
        <Hero
            t={t}
            showBookDemoInHero={true}
            mainTitle={m.mainTitle}
            mainDescription={m.mainDescription}
            leftCard={{
                title: m.heroLeftTitle,
                description: m.heroLeftDescription,
                showButton: true,
            }}
            rightCard={{
                title: m.heroRightTitle,
                description: m.heroRightDescription,
                image: heroImage,
                imageDark: heroImageDark,
                imageClass: "w-full mx-auto object-contain md:h-100",
                altText: m.altHero,
            }}
        >
            <ShopAppAnimation slot="heroVisual" client:only="react" />
        </Hero>
        <Features
            client:load
            t={t}
            features={features}
            title={m.featuresTitle}
            description={m.featuresDescription}
        />
        <Section
            t={t}
            sectionTitle={m.sectionTitle}
            sectionDescription={m.sectionDescription}
            leftCard={{
                title: m.section1Title,
                description: m.section1Description,
                image: shopItem,
                imageClass: "w-1/3 md:w-full mx-auto object-contain md:h-100",
                altText: m.altSection1,
            }}
            rightCard={{
                title: m.section2Title,
                description: m.section2Description,
                altText: m.altSection2,
            }}
        >
            <img
                slot="rightCardVisual"
                src="/gifs/payment-success.gif"
                alt={m.altSection2}
                class="mx-auto md:mt-8 h-40 w-40 object-contain grayscale md:h-48 md:w-48"
            />
        </Section>
        <ProductFAQs t={t} title={m.faqTitle} description={m.faqDescription} faqs={m.faqs} />
    </GSAPSection>
    <SoftwareApplicationSchema
        slot="structured-data"
        name="Edena Shop"
        description={m.metaDescription}
        applicationCategory="EducationalApplication"
        operatingSystem={["Web", "iOS", "Android"]}
        url={`https://edena.es/${lang}/shop`}
        lang={lang}
    />
</Layout>
