---
import type { Translation } from "@/i18n";

interface Props {
    t: Translation;
}

const { t } = Astro.props;
---

<div class="text-center flex flex-col gap-6 items-center pt-16">
    <h2 class="text-2xl font-bold">{t.faqs.title}</h2>
    <p class="max-w-xl">{t.faqs.description}</p>
</div>
