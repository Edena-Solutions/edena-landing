---
import type { Translation } from "@/i18n";

import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardContent, CardFooter } from "@/components/ui/card";
import { Image } from "astro:assets";

import feed from "@/assets/img/feed_app.png"
import notifications from "@/assets/img/notifications_app.png"
import gradient from "@/assets/img/gradient_1.png";
import Link from "@/components/ui/link";
interface Props {
    t: Translation
}

const {
    t,
} = Astro.props;
---

<div class="flex flex-col gap-16 items-center">
    <div class="flex flex-col gap-3 text-center px-4">
        <h2 class="text-3xl font-bold">{t.app.uploadPicturesTitle}</h2>
        <p class="text-muted-foreground max-w-2xl mx-auto">
            {t.app.uploadPicturesDescription}
        </p>
    </div>
    <div class="grid gap-3 px-4 text-sm sm:grid-cols-2 w-full">
        <Card className="flex h-140 flex-col items-center gap-6 overflow-hidden rounded bg-gray-50 text-center">
            <CardHeader className="flex flex-col items-center gap-3">
                <p class="max-w-md">
                    {t.app.communicateAndGetAlbumsDescription}
                </p>
            </CardHeader>
            <CardFooter>
                <Image src={notifications} alt="Mail" class="w-80"/>
            </CardFooter>
        </Card>
        <div class="flex flex-col-reverse md:flex-col gap-3 h-140">
            <Card className="flex min-h-fit rounded"
                style={{
                    backgroundImage: `url('${gradient.src}')`,
                    backgroundSize: "cover",
                    backgroundPosition: "top left",
                }}
            >
                <CardContent className="flex items-center flex-col sm:flex-row gap-3 p-8 justify-between w-full">
                    <p class="max-w-xs text-white font-semibold text-center sm:text-left">
                        {t.setup.bookADemoCTA}
                    </p>
                    <Link href="demo">
                        <Button variant="secondary">
                            {t.bookDemo}
                        </Button>
                    </Link>
                </CardContent>
            </Card>
            <Card className="flex flex-col items-center overflow-hidden rounded bg-gray-50 text-center">
                <CardHeader className="flex flex-col items-center gap-3">
                    <p class="max-w-md">
                        {t.app.uploadPicturesAndVideosDescription}
                    </p>
                </CardHeader>
                <CardFooter>
                    <Image src={feed} alt="Operations" class="w-100"/>
                </CardFooter>
            </Card>
        </div>
    </div>
</div>
